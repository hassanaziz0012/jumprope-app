---
trigger: always_on
---

- When designing new components always follow the `styleguide.md` file in the root of the project. 
- When designing a very large component or piece of UI, try to break it down into reusable components. Put all of the reusable components in the `components` folder. 